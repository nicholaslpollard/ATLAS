name: Temporary Market Mark Docs Reconcile

on:
  push:
    branches:
      - feature/market-mark-evidence

permissions:
  contents: write

jobs:
  reconcile:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/market-mark-evidence
          fetch-depth: 0
      - name: Reconcile README and roadmap
        run: python scripts/_temporary_reconcile_market_mark_docs.py
      - name: Commit living documents and remove temporary helpers
        shell: bash
        run: |
          rm -f scripts/_temporary_reconcile_market_mark_docs.py
          rm -f .github/workflows/_temporary_market_mark_docs.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs/roadmap.md scripts/_temporary_reconcile_market_mark_docs.py .github/workflows/_temporary_market_mark_docs.yml
          git commit -m "Reconcile market-mark evidence architecture"
          git push origin HEAD:feature/market-mark-evidence
