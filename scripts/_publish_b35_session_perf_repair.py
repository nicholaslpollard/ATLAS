from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def read(path: str) -> str:
    return (ROOT / path).read_text(encoding="utf-8")


def write(path: str, text: str) -> None:
    (ROOT / path).write_text(text, encoding="utf-8", newline="\n")


def replace_once(path: str, old: str, new: str) -> None:
    text = read(path)
    count = text.count(old)
    if count != 1:
        raise RuntimeError(f"{path}: expected one occurrence, found {count}: {old[:100]!r}")
    write(path, text.replace(old, new, 1))


# ---------------------------------------------------------------------------
# B35 source: match the accepted V2 writer's four session labels, combine the
# physical/duplicate/session validation into one DuckDB scan, and materialize
# only premarket+regular rows into Pandas because those are the only B35 inputs.
# ---------------------------------------------------------------------------
replace_once(
    "packages/backtesting/b35_development_source.py",
    '''    def __init__(self, settings: AtlasSettings) -> None:\n        self.settings = settings\n        self.layout = V2Layout.beneath((settings.project_root / "data").resolve())''',
    '''    def __init__(self, settings: AtlasSettings, *, duckdb_threads: int = 2) -> None:\n        self.settings = settings\n        self.duckdb_threads = max(1, int(duckdb_threads))\n        self.layout = V2Layout.beneath((settings.project_root / "data").resolve())''',
)

old_validation = '''        con = duckdb.connect(":memory:")\n        con.execute("SET TimeZone='UTC'")\n        con.register("b35_calendar", calendar_frame)\n        symbol_placeholders = ",".join("?" for _ in binding.symbols)\n        exact_source_id = ALPACA_V2_SOURCE_PREFIX + binding.unit_id\n        try:\n            physical_sql = f"""\n                SELECT\n                    count(*)::BIGINT AS rows,\n                    count(*) FILTER (\n                        WHERE session_date IS NULL\n                           OR session_date < ? OR session_date >= ?\n                           OR symbol IS NULL OR symbol NOT IN ({symbol_placeholders})\n                           OR provider IS DISTINCT FROM 'alpaca'\n                           OR dataset IS DISTINCT FROM ?\n                           OR timeframe IS DISTINCT FROM ?\n                           OR is_adjusted IS DISTINCT FROM FALSE\n                           OR source_id IS DISTINCT FROM ?\n                           OR timestamp_utc IS NULL OR provider_timestamp_utc IS NULL\n                           OR timestamp_utc IS DISTINCT FROM provider_timestamp_utc\n                           OR date_trunc('minute', timestamp_utc) IS DISTINCT FROM timestamp_utc\n                           OR open IS NULL OR high IS NULL OR low IS NULL OR close IS NULL\n                           OR volume IS NULL\n                           OR NOT isfinite(CAST(open AS DOUBLE))\n                           OR NOT isfinite(CAST(high AS DOUBLE))\n                           OR NOT isfinite(CAST(low AS DOUBLE))\n                           OR NOT isfinite(CAST(close AS DOUBLE))\n                           OR NOT isfinite(CAST(volume AS DOUBLE))\n                           OR open <= 0 OR high <= 0 OR low <= 0 OR close <= 0\n                           OR volume < 0\n                           OR high < greatest(open, close)\n                           OR low > least(open, close)\n                           OR high < low\n                           OR (vwap IS NOT NULL AND (\n                                  NOT isfinite(CAST(vwap AS DOUBLE)) OR vwap <= 0\n                              ))\n                           OR (transaction_count IS NOT NULL AND transaction_count < 0)\n                    )::BIGINT AS invalid_physical_rows\n                FROM read_parquet(?, hive_partitioning=false)\n            """\n            params: list[object] = [\n                binding.window_start,\n                binding.window_end_exclusive,\n                *binding.symbols,\n                MINUTE_DATASET,\n                MINUTE_TIMEFRAME,\n                exact_source_id,\n                str(binding.canonical_path),\n            ]\n            physical = con.execute(physical_sql, params).fetchone()\n            duplicate = con.execute(\n                """\n                SELECT count(*)::BIGINT\n                FROM (\n                    SELECT symbol, timestamp_utc, timeframe, session_segment, count(*) AS n\n                    FROM read_parquet(?, hive_partitioning=false)\n                    GROUP BY symbol, timestamp_utc, timeframe, session_segment\n                    HAVING count(*) > 1\n                )\n                """,\n                [str(binding.canonical_path)],\n            ).fetchone()\n            segment = con.execute(\n                """\n                SELECT count(*)::BIGINT\n                FROM read_parquet(?, hive_partitioning=false) p\n                LEFT JOIN b35_calendar c USING (session_date)\n                WHERE c.session_date IS NULL\n                   OR p.session_segment IS NULL\n                   OR CASE\n                        WHEN p.timestamp_utc >= c.premarket_start_utc\n                         AND p.timestamp_utc < c.regular_open_utc\n                            THEN p.session_segment <> 'premarket'\n                        WHEN p.timestamp_utc >= c.regular_open_utc\n                         AND p.timestamp_utc < c.regular_close_utc\n                            THEN p.session_segment <> 'regular'\n                        WHEN p.timestamp_utc >= c.regular_close_utc\n                         AND p.timestamp_utc < c.after_hours_end_utc\n                            THEN p.session_segment <> 'after_hours'\n                        ELSE TRUE\n                      END\n                """,\n                [str(binding.canonical_path)],\n            ).fetchone()\n            frame = con.execute(\n                """\n                SELECT *\n                FROM read_parquet(?, hive_partitioning=false)\n                WHERE session_date BETWEEN ? AND ?\n                ORDER BY symbol, session_date, timestamp_utc, session_segment\n                """,\n                [str(binding.canonical_path), start_session, end_session],\n            ).fetchdf()\n        except duckdb.Error as exc:\n            raise B35DevelopmentSourceError(\n                f"canonical minute unit violates B35 physical contract: {binding.unit_id}"\n            ) from exc\n        finally:\n            con.close()\n\n        if "is_adjusted" not in frame.columns or str(frame["is_adjusted"].dtype).lower() not in {\n            "bool",\n            "boolean",\n        }:\n            raise B35DevelopmentSourceError(\n                "B35 canonical schema is_adjusted must be BOOLEAN"\n            )\n        if physical is None or int(physical[1]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains invalid physical rows")\n        if duplicate is None or int(duplicate[0]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains duplicate minute keys")\n        if segment is None or int(segment[0]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains incorrect session labels")\n        return frame'''

new_validation = '''        con = duckdb.connect(":memory:")\n        con.execute("SET TimeZone='UTC'")\n        con.execute(f"PRAGMA threads={self.duckdb_threads}")\n        con.register("b35_calendar", calendar_frame)\n        symbol_placeholders = ",".join("?" for _ in binding.symbols)\n        exact_source_id = ALPACA_V2_SOURCE_PREFIX + binding.unit_id\n        try:\n            validation_sql = f"""\n                WITH source_rows AS (\n                    SELECT\n                        p.*,\n                        c.session_date AS calendar_session_date,\n                        c.premarket_start_utc,\n                        c.regular_open_utc,\n                        c.regular_close_utc,\n                        c.after_hours_end_utc,\n                        count(*) OVER (\n                            PARTITION BY p.symbol, p.timestamp_utc, p.timeframe, p.session_segment\n                        ) AS duplicate_count\n                    FROM read_parquet(?, hive_partitioning=false) p\n                    LEFT JOIN b35_calendar c\n                      ON p.session_date = c.session_date\n                ), checked AS (\n                    SELECT\n                        *,\n                        CASE\n                            WHEN calendar_session_date IS NULL\n                                THEN session_segment = 'closed'\n                            WHEN timestamp_utc >= premarket_start_utc\n                             AND timestamp_utc < regular_open_utc\n                                THEN session_segment = 'premarket'\n                            WHEN timestamp_utc >= regular_open_utc\n                             AND timestamp_utc < regular_close_utc\n                                THEN session_segment = 'regular'\n                            WHEN timestamp_utc >= regular_close_utc\n                             AND timestamp_utc < after_hours_end_utc\n                                THEN session_segment = 'after_hours'\n                            ELSE session_segment = 'closed'\n                        END AS segment_ok\n                    FROM source_rows\n                )\n                SELECT\n                    count(*)::BIGINT AS rows,\n                    count(*) FILTER (\n                        WHERE session_date IS NULL\n                           OR session_date < ? OR session_date >= ?\n                           OR symbol IS NULL OR symbol NOT IN ({symbol_placeholders})\n                           OR provider IS DISTINCT FROM 'alpaca'\n                           OR dataset IS DISTINCT FROM ?\n                           OR timeframe IS DISTINCT FROM ?\n                           OR is_adjusted IS DISTINCT FROM FALSE\n                           OR source_id IS DISTINCT FROM ?\n                           OR timestamp_utc IS NULL OR provider_timestamp_utc IS NULL\n                           OR timestamp_utc IS DISTINCT FROM provider_timestamp_utc\n                           OR date_trunc('minute', timestamp_utc) IS DISTINCT FROM timestamp_utc\n                           OR open IS NULL OR high IS NULL OR low IS NULL OR close IS NULL\n                           OR volume IS NULL\n                           OR NOT isfinite(CAST(open AS DOUBLE))\n                           OR NOT isfinite(CAST(high AS DOUBLE))\n                           OR NOT isfinite(CAST(low AS DOUBLE))\n                           OR NOT isfinite(CAST(close AS DOUBLE))\n                           OR NOT isfinite(CAST(volume AS DOUBLE))\n                           OR open <= 0 OR high <= 0 OR low <= 0 OR close <= 0\n                           OR volume < 0\n                           OR high < greatest(open, close)\n                           OR low > least(open, close)\n                           OR high < low\n                           OR (vwap IS NOT NULL AND (\n                                  NOT isfinite(CAST(vwap AS DOUBLE)) OR vwap <= 0\n                              ))\n                           OR (transaction_count IS NOT NULL AND transaction_count < 0)\n                    )::BIGINT AS invalid_physical_rows,\n                    count(*) FILTER (WHERE duplicate_count > 1)::BIGINT AS duplicate_member_rows,\n                    count(*) FILTER (WHERE NOT segment_ok)::BIGINT AS incorrect_session_rows\n                FROM checked\n            """\n            params: list[object] = [\n                str(binding.canonical_path),\n                binding.window_start,\n                binding.window_end_exclusive,\n                *binding.symbols,\n                MINUTE_DATASET,\n                MINUTE_TIMEFRAME,\n                exact_source_id,\n            ]\n            validation = con.execute(validation_sql, params).fetchone()\n            frame = con.execute(\n                """\n                SELECT *\n                FROM read_parquet(?, hive_partitioning=false)\n                WHERE session_date BETWEEN ? AND ?\n                  AND session_segment IN ('premarket', 'regular')\n                ORDER BY symbol, session_date, timestamp_utc, session_segment\n                """,\n                [str(binding.canonical_path), start_session, end_session],\n            ).fetchdf()\n        except duckdb.Error as exc:\n            raise B35DevelopmentSourceError(\n                f"canonical minute unit violates B35 physical contract: {binding.unit_id}"\n            ) from exc\n        finally:\n            con.close()\n\n        if "is_adjusted" not in frame.columns or str(frame["is_adjusted"].dtype).lower() not in {\n            "bool",\n            "boolean",\n        }:\n            raise B35DevelopmentSourceError(\n                "B35 canonical schema is_adjusted must be BOOLEAN"\n            )\n        if validation is None or int(validation[1]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains invalid physical rows")\n        if int(validation[2]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains duplicate minute keys")\n        if int(validation[3]) != 0:\n            raise B35DevelopmentSourceError("B35 canonical unit contains incorrect session labels")\n        return frame'''
replace_once("packages/backtesting/b35_development_source.py", old_validation, new_validation)

# ---------------------------------------------------------------------------
# Runner: resolve and display the bounded hardware profile. It is operational
# metadata only and does not alter the frozen scientific fingerprints.
# ---------------------------------------------------------------------------
replace_once(
    "scripts/run_b35_development_replay.py",
    '''from packages.core.atomic_io import atomic_write_text\nfrom packages.core.settings import AtlasSettings, load_settings''',
    '''from packages.core.atomic_io import atomic_write_text\nfrom packages.core.execution_profile import resolve_research_execution_profile\nfrom packages.core.settings import AtlasSettings, load_settings''',
)
replace_once(
    "scripts/run_b35_development_replay.py",
    '''    settings = load_settings(PROJECT_ROOT)\n    source = B35DevelopmentMinuteSource(settings)\n    plan = source.plan(start, end)''',
    '''    settings = load_settings(PROJECT_ROOT)\n    execution_profile = resolve_research_execution_profile()\n    source = B35DevelopmentMinuteSource(\n        settings,\n        duckdb_threads=execution_profile.duckdb_threads,\n    )\n    plan = source.plan(start, end)''',
)
replace_once(
    "scripts/run_b35_development_replay.py",
    '''    print(f"  B35 contract fingerprint: {B35_PREOUTCOME_FINGERPRINT}")\n    print("  consumed master rows permitted/read: 0 / 0")''',
    '''    print(f"  B35 contract fingerprint: {B35_PREOUTCOME_FINGERPRINT}")\n    memory_gib = execution_profile.as_dict()["total_memory_gib"]\n    print(\n        "  execution profile: "\n        f"{execution_profile.logical_cpus} logical CPUs, "\n        f"{memory_gib if memory_gib is not None else 'unknown'} GiB RAM, "\n        f"DuckDB threads={execution_profile.duckdb_threads} "\n        f"({execution_profile.profile_source})"\n    )\n    print("  consumed master rows permitted/read: 0 / 0")''',
)

# ---------------------------------------------------------------------------
# Source regressions: writer-valid closed/after-hours rows validate but are not
# materialized into the B35 strategy frame; mismatched labels still fail closed.
# ---------------------------------------------------------------------------
replace_once(
    "tests/unit/test_b35_development_source.py",
    '''def _write_valid_parquet(\n    path: Path,\n    *,\n    session_segment: str = "regular",\n    adjusted: object = False,\n    duplicate: bool = False,\n) -> None:\n    stamp = datetime(2026, 4, 30, 13, 30, tzinfo=UTC)  # 09:30 ET''',
    '''def _write_valid_parquet(\n    path: Path,\n    *,\n    session_segment: str = "regular",\n    adjusted: object = False,\n    duplicate: bool = False,\n    timestamp_utc: datetime | None = None,\n) -> None:\n    stamp = timestamp_utc or datetime(2026, 4, 30, 13, 30, tzinfo=UTC)  # 09:30 ET''',
)
replace_once(
    "tests/unit/test_b35_development_source.py",
    '''def _write_fixture(\n    tmp_path: Path,\n    *,\n    write_checkpoint: bool = True,\n    session_segment: str = "regular",\n    adjusted: object = False,\n    duplicate: bool = False,\n) -> B35DevelopmentMinuteSource:''',
    '''def _write_fixture(\n    tmp_path: Path,\n    *,\n    write_checkpoint: bool = True,\n    session_segment: str = "regular",\n    adjusted: object = False,\n    duplicate: bool = False,\n    timestamp_utc: datetime | None = None,\n) -> B35DevelopmentMinuteSource:''',
)
replace_once(
    "tests/unit/test_b35_development_source.py",
    '''    _write_valid_parquet(\n        canonical,\n        session_segment=session_segment,\n        adjusted=adjusted,\n        duplicate=duplicate,\n    )''',
    '''    _write_valid_parquet(\n        canonical,\n        session_segment=session_segment,\n        adjusted=adjusted,\n        duplicate=duplicate,\n        timestamp_utc=timestamp_utc,\n    )''',
)

append_anchor = '''def test_load_unit_accepts_exact_valid_physical_rows(tmp_path: Path) -> None:\n    source = _write_fixture(tmp_path)\n    binding = source.plan(date(2026, 4, 1), date(2026, 4, 30)).units[0]\n    frame = source.load_unit(\n        binding,\n        start_session=date(2026, 4, 1),\n        end_session=date(2026, 4, 30),\n    )\n    assert len(frame) == 1\n    assert frame.iloc[0]["symbol"] == "TEST"\n\n\n'''
append_tests = append_anchor + '''def test_load_unit_accepts_writer_valid_closed_row_without_materializing_it(\n    tmp_path: Path,\n) -> None:\n    source = _write_fixture(\n        tmp_path,\n        session_segment="closed",\n        timestamp_utc=datetime(2026, 4, 30, 7, 59, tzinfo=UTC),  # 03:59 ET\n    )\n    binding = source.plan(date(2026, 4, 1), date(2026, 4, 30)).units[0]\n    frame = source.load_unit(\n        binding,\n        start_session=date(2026, 4, 1),\n        end_session=date(2026, 4, 30),\n    )\n    assert frame.empty\n\n\ndef test_load_unit_accepts_writer_valid_after_hours_without_materializing_it(\n    tmp_path: Path,\n) -> None:\n    source = _write_fixture(\n        tmp_path,\n        session_segment="after_hours",\n        timestamp_utc=datetime(2026, 4, 30, 20, 0, tzinfo=UTC),  # 16:00 ET\n    )\n    binding = source.plan(date(2026, 4, 1), date(2026, 4, 30)).units[0]\n    frame = source.load_unit(\n        binding,\n        start_session=date(2026, 4, 1),\n        end_session=date(2026, 4, 30),\n    )\n    assert frame.empty\n\n\n'''
replace_once("tests/unit/test_b35_development_source.py", append_anchor, append_tests)

# ---------------------------------------------------------------------------
# Living documentation: record the first authorized read attempt, exact failure,
# and the repair/performance behavior without changing scientific authority.
# ---------------------------------------------------------------------------
replace_once(
    "README.md",
    '''- **B35 source-only workstation preflight is ACCEPTED; DEVELOPMENT outcomes remain unopened.** On merged `main` commit `4cf27ae9d23ac3b5d1821200e4d303db787f361a`, the governed `2016-01-04..2026-04-30` preflight selected exactly `59,768` frozen minute units with source fingerprint `af371478a68d2dca486ffbaa1a339d24e1166257a67ba5f6ededfdda202a04cd`, split-evidence fingerprint `3bcccaddf3937368c675fb27a441f9bbac8cbf34816c26e37a4a3e84d03554f6`, and active B35 contract fingerprint `145cc8983439b6062fd5e60303539d1611cdf7db0119ed68a7da1e68ffd8eda6`. The preflight created no outcome authorization and opened no outcomes. Consumed-master rows permitted/read = `0/0`; future-blind rows permitted/read = `0/0`; provider calls/broker reads/broker writes = `0/0/0`; PAPER/LIVE authority = `false/false`; strategy/selector promotion remains `false/false`. The consumed `2026-05-12..2026-08-11` master interval remains permanently unavailable to B35 fitting/scoring, and the future blind beginning on/after `2026-09-08` remains unopened. The next Track-B gate is the single canonical `--authorize-development-outcomes` replay, followed by strategy × condition analysis and the preregistered walk-forward selector/profile evidence; no alternate date/output/ledger scope is permitted.''',
    '''- **B35 source-only preflight is ACCEPTED; the first authorized DEVELOPMENT read attempt is preserved as an interrupted run and is being repaired without changing the frozen experiment.** The preflight selected exactly `59,768` minute units with source fingerprint `af371478a68d2dca486ffbaa1a339d24e1166257a67ba5f6ededfdda202a04cd`, split-evidence fingerprint `3bcccaddf3937368c675fb27a441f9bbac8cbf34816c26e37a4a3e84d03554f6`, and active B35 fingerprint `145cc8983439b6062fd5e60303539d1611cdf7db0119ed68a7da1e68ffd8eda6`. On `main` `1d00fb9d362f46c226972ae4d92c45c2fd395413`, explicit DEVELOPMENT authorization `562d7104d56151e6203a1bf85457f9d1a90bbf19e60cd4d9359a3f96bc0a7be5` and registration `b35.dev.20160104_20260430.eb3b7ff9f417.registration` were created before the run stopped on `B35 canonical unit contains incorrect session labels`. Review traced this to a validator/writer mismatch: the accepted V2 writer intentionally persists provider bars outside the extended-session clock as `session_segment='closed'`, while the B35 validator incorrectly rejected that fourth canonical label. The repair validates `closed` against the exact writer clock, still rejects mislabeled rows, and materializes only premarket/regular rows into strategy memory. It also adds a hardware-aware research execution profile; on a 12-logical-CPU/24-GiB host the default is eight DuckDB threads while reserving host headroom, and validation scans are collapsed to reduce repeated Parquet I/O. The same immutable authorization/trial identity is reused on restart; no alternate scope or scientific rule is introduced. Consumed-master and future-blind rows remain `0`; provider/broker/PAPER/LIVE authority remains absent; strategy/selector promotion remains `false`.''',
)

roadmap_text = read("docs/roadmap.md")
needle = "The next Track-B gate is the single canonical `--authorize-development-outcomes` replay"
if needle not in roadmap_text:
    raise RuntimeError("roadmap B35 next-gate anchor not found")
roadmap_text = roadmap_text.replace(
    needle,
    "The first single canonical `--authorize-development-outcomes` attempt created its immutable authorization/registration but stopped before completion on a B35 validator mismatch with the accepted V2 `closed` session label; after exact-head repair acceptance, the same canonical command must resume under the same authorization. The next Track-B gate after completion is strategy × condition analysis",
    1,
)
write("docs/roadmap.md", roadmap_text)

# Self-delete publisher machinery from the final tree.
(ROOT / "scripts/_publish_b35_session_perf_repair.py").unlink(missing_ok=True)
(ROOT / ".github/workflows/_publish_b35_session_perf_repair.yml").unlink(missing_ok=True)
